plugin: amazon.aws.aws_ec2
regions:
  - eu-west-2

filters:
  tag:Name: DevOpsEC2

keyed_groups:
  - key: tags.Name
    prefix: tag

hostnames:
  - ip-address
strict: false  # Allow fallback groups for broader discovery
